<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Clubhouse Collective</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%# <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <style>
      html, body {
        font-family: system-ui, sans-serif;
        margin: 0;
        padding: 0;
        color: #333;
        background: #fff;
        line-height: 1.6;
        flex-direction: column;
      }

      header {
        background: #1f2937;
        color: #fff;
        padding: 1rem 0;
      }
      .header-container {
        max-width: 1200px;
        margin: auto;
        padding: 0 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .logo a {
        font-size: 1.5rem;
        font-weight: bold;
        color: inherit;
        text-decoration: none;
      }
      nav ul {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
      }
      nav li + li {
        margin-left: 1rem;
      }
      nav a {
        color: inherit;
        text-decoration: none;
      }
      nav a:hover {
        color: #3b82f6;
      }

      .notice, .alert {
        margin: 1rem;
        padding: 0.75rem 1rem;
        border-radius: 0.375rem;
      }
      .notice {
        background: #d1fae5;
      }
      .alert {
        background: #fee2e2;
      }

      footer {
        background: #1f2937;
        color: #d1d5db;
        padding: 2rem 0;
      }
      .footer-container {
        max-width: 1200px;
        margin: auto;
        padding: 0 1rem;
        display: grid;
        gap: 1.5rem;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      }
      .footer-section h3 {
        font-size: 1.125rem;
        margin-bottom: 1rem;
      }
      .footer-section a {
        color: #9ca3af;
        text-decoration: none;
      }
      .footer-section a:hover {
        color: #3b82f6;
      }
      .copyright {
        text-align: center;
        margin-top: 2rem;
        font-size: 0.875rem;
        color: #6b7280;
      }

      .breadcrumb {
        font-size: 0.875rem;
        color: #6b7280;
        margin: 1rem 0;
      }
      .breadcrumb a {
        color: #3b82f6;
        text-decoration: none;
      }
      .breadcrumb a:hover {
        text-decoration: underline;
      }
      .product-container {
        display: grid;
        gap: 2rem;
        grid-template-columns: 1fr 1fr;
        margin-bottom: 3rem;
      }
      .main-image {
        width: 100%;
        height: 400px;
        background: #f9fafb;
        border-radius: 0.5rem;
        overflow: hidden;
      }
      .main-image img {
        width: 100%;
        height: 100%;
        object-fit: contain;
      }
      .thumbnail-images {
        display: flex;
        gap: 0.5rem;
        overflow-x: auto;
        margin-top: 1rem;
      }
      .thumbnail {
        width: 80px;
        height: 80px;
        object-fit: cover;
        border-radius: 0.25rem;
        cursor: pointer;
        border: 2px solid transparent;
      }
      .thumbnail:hover {
        border-color: #3b82f6;
      }
      .product-info h1 {
        font-size: 1.875rem;
        margin-bottom: 1rem;
        color: #111827;
      }
      .product-description h3 {
        font-size: 1.125rem;
        margin-bottom: 0.5rem;
      }
      .stock-info {
        margin: 1rem 0;
      }
      .in-stock {
        color: #10b981;
        font-weight: 600;
      }
      .out-of-stock {
        color: #ef4444;
        font-weight: 600;
      }
      .add-to-cart-btn {
        background: #3b82f6;
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 0.375rem;
        font-weight: bold;
        cursor: pointer;
      }
      .add-to-cart-btn:hover {
        background: #2563eb;
      }

      .related-products {
        margin-top: 3rem;
      }
      .related-products h2 {
        font-size: 1.5rem;
        margin-bottom: 1rem;
      }
      .related-products-grid {
        display: grid;
        gap: 1.5rem;
        grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
      }
      .related-product-card {
        border: 1px solid #e5e7eb;
        border-radius: 0.375rem;
        overflow: hidden;
        transition: transform 0.2s ease;
      }
      .related-product-card:hover {
        transform: translateY(-4px);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
      }
      .related-product-card h3 {
        padding: 0.75rem;
        font-size: 0.875rem;
        color: #111827;
      }

      .category-sidebar h3 {
        margin-top: 2rem;
      }
      .category-sidebar ul {
        list-style: none;
        padding: 0;
      }
      .category-sidebar li {
        margin-bottom: 0.75rem;
      }
      .category-sidebar a {
        color: #3b82f6;
        text-decoration: none;
      }
      .category-sidebar a:hover {
        color: #2563eb;
      }

      .cart-page {
        margin-top: 2rem;
      }
      .cart-page h1 {
        margin-bottom: 2rem;
      }
      .cart-table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 2rem;
      }
      .cart-table th, .cart-table td {
        padding: 1rem;
        border-bottom: 1px solid #e5e7eb;
      }
      .cart-thumbnail {
        width: 80px;
        height: 80px;
        object-fit: contain;
        background: #f9fafb;
      }
      .quantity-input {
        width: 60px;
        padding: 0.5rem;
        border: 1px solid #e5e7eb;
        border-radius: 0.375rem;
        text-align: center;
      }
      .update-btn, .remove-btn, .empty-cart-btn, .continue-shopping-btn, .checkout-btn {
        padding: 0.5rem 1rem;
        border-radius: 0.375rem;
        font-size: 0.875rem;
        font-weight: 500;
        border: none;
        cursor: pointer;
      }
      .update-btn {
        background: #9ca3af;
        color: white;
      }
      .update-btn:hover {
        background: #6b7280;
      }
      .remove-btn, .empty-cart-btn {
        background: #ef4444;
        color: white;
      }
      .remove-btn:hover, .empty-cart-btn:hover {
        background: #dc2626;
      }
      .continue-shopping-btn {
        background: #6b7280;
        color: white;
      }
      .continue-shopping-btn:hover {
        background: #4b5563;
      }
      .checkout-btn {
        background: #10b981;
        color: white;
      }
      .checkout-btn:hover {
        background: #059669;
      }

      @media (max-width: 768px) {
        .header-container, .footer-container {
          flex-direction: column;
          text-align: center;
        }
        nav ul {
          flex-direction: column;
          gap: 0.75rem;
          margin-top: 1rem;
        }
        .product-container {
          grid-template-columns: 1fr;
        }
        .related-products-grid {
          grid-template-columns: 1fr 1fr;
        }
      }
    </style>
  </head>

  <body>
  <header>
    <div class="header-container">
      <div class="logo">
        <%= link_to "Clubhouse Collective", root_path %>
      </div>
      <nav>
        <ul>
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "Products", products_path %></li>
          <li><%= link_to "About", "/about" %></li>
          <li><%= link_to "Contact", "/contact" %></li>
          <li class="cart-link">
            <%= link_to cart_path do %>
              Cart
              <% if session[:cart].present? && !session[:cart].empty? %>
                <span class="cart-count"><%= session[:cart].sum { |item| item[:quantity].to_i } %></span>
              <% end %>
            <% end %>
          </li>
        </ul>
      </nav>
    </div>
  </header>

    <% if notice %>
      <div class="notice"><%= notice %></div>
    <% end %>
    <% if alert %>
      <div class="alert"><%= alert %></div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <footer>
      <div class="footer-container">
        <div class="footer-section">
          <h3>Clubhouse Collective</h3>
          <p>Premium golf gear and streetwear for the modern golfer.</p>
        </div>

        <nav class="footer-section">
          <h3>Quick Links</h3>
          <ul>
            <li><%= link_to "Home", root_path %></li>
            <li><%= link_to "Products", products_path %></li>
            <li><%= link_to "About", "/about" %></li>
            <li><%= link_to "Contact", "/contact" %></li>
            <li class="cart-link">
                <%= link_to cart_path do %>
                  Cart
                  <% if session[:cart].present? && !session[:cart].empty? %>
                    <span class="cart-count"><%= session[:cart].sum { |item| item[:quantity].to_i } %></span>
                  <% end %>
                <% end %>
            </li>
          </ul>
        </nav>

        <div class="footer-section">
          <h3>Contact Us</h3>
          <p>Email: info@clubhousecollective.com</p>
          <p>Phone: (204) 555-1234</p>
        </div>
      </div>

      <div class="copyright">
        &copy; <%= Date.today.year %> Clubhouse Collective. All Rights Reserved.
      </div>
    </footer>

  </body>
</html>